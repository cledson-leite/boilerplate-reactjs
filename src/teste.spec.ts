import { Teste } from '@/teste'

describe('teste', () => {
  it('teste', () => {
    //produz os dados do teste
    const sut = new Teste()
    //operacionar esses dados
    
    //verificar resultado esperado
    expect(1).toBe(1)
  })
})